# Usage 

Input people, blockouts, and daily needs at the top of main()

Re-order the objectives if you want to

Relax the tolerances if solutions are not found

Run the thing and take the resulting output as CSV to use in excel